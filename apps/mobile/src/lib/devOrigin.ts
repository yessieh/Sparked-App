// Dev test location: Sahuarita, AZ — shared by Explore (feed radius) and
// Event Detail (distance line). Device geolocation replaces this at a later
// stage; keeping one constant means both surfaces agree on every distance.

export const TEST_ORIGIN = { lat: 31.9576, lng: -110.9556 };
